{
  "name": "cpap-mask-selector",
  "version": "1.0.0",
  "description": "CPAP Mask Selector - Local Server App",
  "main": "server/index.js",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "node server/index.js",
    "dev": "npm run build && nodemon server/index.js",
    "setup": "npm run install-all && npm run build"
  },
  "keywords": [
    "cpap",
    "mask",
    "selector"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
